#see http://stasis.me

#install stasis
gem install bundler
gem install stasis
gem install haml
gem install less
gem install therubyracer

#dev
compile => $ stasis (public folder)
deploy => stasis -d 3000(port)

go localhost:3000
